fastapi>=0.115.0
uvicorn[standard]>=0.30.0
numpy>=1.26.0
soundfile>=0.12.1
python-multipart>=0.0.9
httpx>=0.27.0

# Install OmniVoice and torch/torchaudio separately; see ../README.md

# For speech-to-text (Whisper). Requires `ffmpeg` installed locally.
openai-whisper>=20240930
